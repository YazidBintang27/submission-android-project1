<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SubmissionProject" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/grey_black</item>
        <item name="android:windowBackground">@color/grey_black</item>
        <item name="actionBarStyle">@style/Base.Theme.ActionBar</item>

    </style>

    <style name="Base.Theme.ActionBar" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="android:background">@color/blue</item>
    </style>
</resources>